# cat

> Print and concatenate files to `stdout`.
> See http://example.com for more information.
> More information: <https://example.com/cat>.

- Print the contents of a file to `stdout`:

`cat {{path/to/file}}`

- See http://example.com for details:

`cat {{path/to/file}}`
